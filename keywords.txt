Bash reserved words and selected build-ins (25):

  if  then  elif  for  do     until  select  case  function  time   shift
  fi        else  in   done   while          esac  exit      eval
  export               break  continue             return    unset  umask

Old C reserved keywords (32):

  auto   const     double  float  int       short   struct   unsigned
  break  continue  else    for    long      signed  switch   void
  case   default   enum    goto   register  sizeof  typedef  volatile
  char   do        extern  if     return    static  union    while

New to C++ (30):

  asm          dynamic_cast   namespace   reinterpret_cast   try
  bool         explicit       new         static_cast        typeid
  catch        false          operator    template           typename
  class        friend         private     this               using
  const_cast   inline         public      throw              virtual
  delete       mutable        protected   true               wchar_t

Non-essential C++ reserved words (may be not implemented, not included):

  and      bitand   compl   not_eq   or_eq   xor_eq
  and_eq   bitor            not      or      xor

Preprocessor directives (not really reserved, but common in C code):

  define    ifdef   ifndef  |
  include   endif   elif    | NOTE: if and else are already C keywords

Lua keywords (21):

  and       break     do      else        elseif
  end       false     for     function    if
  in        local     nil     not         or
  repeat    return    then    true        until     while

Perl keywords and stuff (30):

  and  eq     my      if     while   foreach   last  use   strict  require
  or   ne     sub     else   until   for       next  do    print   shift
  die  undef  return  elsif  unless  continue  redo  goto  printf  unshift

Python keywords and constant name (32):

  and        def      finally   in       print    yield   None
  as         del      for       is       raise
  assert     elif     from      lambda   return
  break      else     global    not      try
  class      except   if        or       while
  continue   exec     import    pass     with

